import type { Metadata } from "next";
import { Inter } from "next/font/google";
import "./globals.css";

const inter = Inter({
  subsets: ["latin"],
  variable: "--font-inter",
});

export const metadata: Metadata = {
  title: "JobPilot — AI-Powered Job Application Platform",
  description:
    "Track applications, automate follow-ups, parse job descriptions with AI, and manage your entire job search from one premium dashboard.",
  keywords: ["job tracker", "application management", "AI", "career", "dashboard"],
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en" className="dark">
      <body className={`${inter.variable} font-sans antialiased`}>
        {children}
      </body>
    </html>
  );
}
